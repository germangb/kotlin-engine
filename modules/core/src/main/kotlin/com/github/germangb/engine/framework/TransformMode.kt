package com.github.germangb.engine.framework

enum class TransformMode {
    /**
     * Transformations are relative bind that of the parent Actor
     */
    RELATIVE,

    /**
     * Transformations are given in absolute terms
     */
    ABSOLUTE
}