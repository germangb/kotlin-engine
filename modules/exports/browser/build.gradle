compileKotlin2Js {
    kotlinOptions.verbose = true
    kotlinOptions.moduleKind = "plain"
}

dependencies {
    compile project(":modules:backends:browser")
    compile project(":modules:game")
    compile project(":modules:core")
}